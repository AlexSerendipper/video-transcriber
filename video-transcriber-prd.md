# 本地视频转文字工具产品需求文档

## 1. 产品定位

本产品是运行在个人 Windows 电脑上的本地视频转文字工具。用户可以先导入并播放本地媒体，在确有需要时再启动语音转写。视频副本、历史记录和文字稿均保存在本机，不依赖第三方会议纪要网站。

产品形态：

```text
本地 Python 后端 + 浏览器前端 + faster-whisper + 本地文件历史
```

默认访问地址为 `http://127.0.0.1:8876`。

## 2. 目标用户与核心目标

目标用户是希望在本机整理中文视频、课程、访谈或音频材料的个人用户。

核心目标：

- 媒体导入后立即可播放，不强制等待转写。
- 导入和转写是两个独立、可理解的动作。
- 已导入但尚未转写的视频可以长期保留在历史中。
- 转写状态、进度和失败信息集中展示在文字稿区域。
- 用户可以在视频和文字稿之间调整横向占比，同时保持文字稿纵向铺满。
- 重复导入、刷新页面和重新启动程序时，已有历史不会丢失。

## 3. 非目标

当前版本不包含：

- 云端上传、账号体系或多人协作；
- 多任务并行转写；
- 在线分享或远程访问；
- 一键复制全文按钮；
- 视频剪辑、字幕烧录或导出字幕文件；
- 在转写进行中切换或删除其他历史记录。

文字稿仍允许用户选择部分文本并使用系统复制功能。

## 4. 核心用户流程

```text
启动应用
→ 导入视频
→ 视频立即可播放并写入历史
→ 文字稿区域显示“未转写”
→ 用户选择识别模式并点击“开始转写”
→ 文字稿区域显示进度与终止按钮
→ 转写完成后展示可交互文字稿
```

已完成记录允许重新转写：

```text
打开已完成记录
→ 按钮显示“重新转写”
→ 用户点击
→ 弹出覆盖确认
→ 用户确认后清空旧文字稿并开始新任务
```

## 5. 状态模型

### 5.1 持久状态

| 状态 | 含义 | 视频 | 文字稿 | 主操作 |
|---|---|---|---|---|
| `untranscribed` | 已导入但没有文字稿 | 可播放 | 显示“未转写” | 开始转写 |
| `done` | 转写已完成 | 可播放 | 展示文字稿 | 重新转写 |

### 5.2 运行状态

| 状态 | 含义 | 允许操作 |
|---|---|---|
| `queued` | 任务已创建，等待执行 | 播放、终止 |
| `running` | 正在识别或处理文字 | 播放、终止 |
| `cancelling` | 已请求终止，等待安全停止 | 播放 |

状态转换：

```text
导入成功 → untranscribed
untranscribed → queued → running → done
done + 确认重新转写 → queued → running → done
queued/running + 用户终止 → cancelling
cancelling + 后端安全停止 → untranscribed
queued/running + 转写异常 → untranscribed
```

终止或失败不得删除已导入的视频副本和历史记录。

用户确认重新转写后，旧文字稿在新任务创建前立即清空。新任务终止或失败时旧文字稿不恢复，记录进入 `untranscribed`。

失败原因保存在当前后端内存状态中。同一后端进程仍将该记录作为当前记录时，刷新页面应从 `/api/status` 恢复失败原因；重新打开该记录、切换记录或重启后端后，临时错误信息清除，只保留持久状态 `untranscribed`，界面显示“未转写”。

## 6. 功能需求

### 6.1 视频导入

- 顶部操作区提供“导入本地视频”和“链接导入”按钮，本地文件仍保持一次点击选择。
- 文件选择完成后立即上传到本地后端。
- 链接导入接受纯 URL 或包含 URL 的完整分享文本，提取其中第一个 HTTP/HTTPS 链接。
- 首版链接导入支持抖音、Bilibili、YouTube 和 TikTok，只处理单个视频，不处理播放列表。
- 链接导入按“解析链接、获取视频信息、下载视频、写入历史”显示阶段进度，并允许安全终止。
- 链接导入期间保留当前视频播放，但禁止再次导入、启动转写、切换历史和删除历史。
- 链接解析或下载失败时在对话框内显示原因、保留输入内容并允许重试；失败和终止均清理临时文件。
- 后端以完整文件内容的 SHA-256 作为历史标识。
- 新文件保存至 `data/items/<file_hash>/source.<extension>`。
- 新记录立即写入 `result.json`，状态为 `untranscribed`。
- 导入成功后自动选中该记录并加载播放器。
- 同一内容再次导入时直接打开已有记录，不重复保存、不覆盖文字稿。
- 链接记录额外保存公开规范页面 URL、平台和平台视频 ID，并优先按平台视频 ID 避免重复下载。
- 链接记录的历史展示名使用解析器返回的原始视频标题，不使用临时下载文件名。
- Windows 下调用链接解析器时必须显式使用 UTF-8 输入输出环境，中文标题不得因系统代码页产生乱码或替换字符。
- 链接媒体命中相同内容哈希时，应刷新已有记录的展示名和公开来源元数据，但不得覆盖已有文字稿。
- 不保存 cookie、请求头、临时播放地址或完整分享文案。
- 转写任务活跃期间禁止导入。

### 6.2 历史抽屉

- 历史栏默认为收起状态。
- 通过主标题左侧的历史图标打开。
- 抽屉覆盖主工作区，不得改变播放器或文字稿尺寸。
- 支持按视频文件名实时筛选。
- 历史卡片显示文件名及“未转写 / 转写中 / 时长”等状态。
- 当前记录使用高亮样式。
- 选择记录后抽屉保持打开，由用户手动关闭。
- 支持收起按钮、遮罩点击和 `Esc` 关闭。
- 转写期间禁止切换历史和删除记录。
- 转写期间仍允许打开、筛选和关闭历史抽屉，但历史卡片的打开与删除按钮必须禁用。

### 6.3 视频播放

- 导入完成后无需转写即可播放。
- 视频保持 `16:9`，在左侧区域顶部对齐，不允许纵向拉伸。
- 每条历史记录独立保存浏览器播放位置。
- 刷新页面后恢复最后打开的记录和播放位置。
- “允许播放”包括播放、暂停、拖动媒体进度和调整播放器音量。

### 6.4 转写

- 保留速度优先、平衡、准确优先三个识别模式。
- 识别模式只在启动转写时读取。
- 未转写记录显示“开始转写”。
- 已完成记录显示“重新转写”。
- 重新转写必须先显示确认框，用户确认后才能覆盖旧文字稿。
- 单个后端进程同一时间只允许一个转写任务。
- 转写期间禁止再次启动转写或重新转写。

### 6.5 转写进度与终止

- 页面不设置独立的全局进度栏。
- 进度百分比、阶段说明、引擎信息、降级原因和“终止转写”均显示在文字稿正文区域。
- 转写进行时，文字稿标题栏保持可见。
- 用户终止后保留视频，清空未完成文字稿并回到 `untranscribed`。
- 转写异常时显示失败原因，历史状态回到 `untranscribed`。
- 页面刷新后，如果任务仍在运行，应恢复当前视频和实时进度。
- `cancelling` 阶段保留终止按钮但将其禁用，状态文字显示正在等待安全停止。

### 6.6 文字稿

- 未导入视频时显示“请先导入视频”。
- 已导入但未转写时显示“未转写”。
- 转写完成后按段落展示文字稿。
- 点击段落跳转到对应视频时间。
- 用户选择文字时不得触发跳转。
- 标题栏固定在文字稿区域顶部。
- 内容较长时只滚动正文，不滚动标题栏或整个页面。

### 6.7 布局与分隔条

- 主工作区使用完整视口宽高。
- 右侧文字稿面板从工作区顶部延伸到窗口底部。
- 左右分隔条只改变播放器和文字稿的横向占比。
- 拖动分隔条不得改变文字稿纵向高度。
- 视频保持顶部 `16:9`；视频下方允许出现空白，不强制拉伸播放器。

## 7. 后端接口

| 方法 | 路径 | 用途 |
|---|---|---|
| `POST` | `/api/import` | 导入并持久化媒体，重复内容返回已有记录 |
| `POST` | `/api/link-import` | 创建单视频链接解析与下载任务 |
| `GET` | `/api/link-import/{task_id}` | 获取链接导入阶段、进度和结果 |
| `POST` | `/api/link-import/{task_id}/cancel` | 请求终止链接导入任务 |
| `POST` | `/api/history/{file_hash}/transcribe` | 对已导入媒体开始转写或重新转写 |
| `POST` | `/api/cancel` | 请求终止当前任务 |
| `GET` | `/api/status` | 获取当前任务、进度及 `api_version` |
| `GET` | `/api/result` | 获取当前已完成文字稿 |
| `GET` | `/api/video` | 播放当前选中媒体 |
| `GET` | `/api/history` | 获取历史摘要列表 |
| `POST` | `/api/history/{file_hash}/open` | 打开历史记录并返回状态摘要 |
| `DELETE` | `/api/history/{file_hash}` | 删除历史记录和工具保存的媒体副本 |

活跃任务期间，导入、打开历史和删除历史接口必须返回 `409 Conflict`。

## 8. 本地数据

历史目录：

```text
data/items/<file_hash>/
├── source.<extension>
└── result.json
```

`result.json` 主要字段：

```text
hash
video_name
source_file
duration
created_at
transcribed_at
status
segments[]
source_type
source_url
source_platform
source_id
source_title
```

转写完成、重新转写、转写失败或终止时，应保留 `source_type / source_url / source_platform / source_id / source_title` 等链接来源字段。

旧版没有 `status` 字段的记录应兼容读取；只要存在 `segments` 字段，即视为已完成记录。

## 9. 启动与版本兼容

- 后端通过 `/api/status` 返回 `api_version`。
- 当前前后端协议版本为 `3`。
- 启动脚本只复用版本匹配的现有服务。
- 启动脚本通过 `/openapi.json` 的 `info.title = Local Video Transcriber` 确认端口服务属于本项目，并通过监听进程命令行再次确认目标是对应端口的 `uvicorn app.main:app`。
- 如果端口服务无法返回 JSON、没有 OpenAPI 标题或身份无法确认，启动脚本不得终止该进程，应报错退出。
- 如果本项目旧版后端无法返回已知状态，启动脚本不得推断其空闲，应报错退出。
- 如果端口上运行的是本项目旧版后端，且状态属于已知空闲集合 `idle / done / untranscribed / error / cancelled`，启动脚本可以停止旧进程并启动新版。
- 如果旧版仍在转写，启动脚本不得强制终止，应提示用户先结束任务。
- 如果端口属于其他进程，启动脚本不得终止该进程。
- 浏览器只在目标版本后端准备完成后打开。
- 前端直接连接到缺少 `api_version` 或版本不为 `3` 的后端时，应禁用导入、转写、历史切换和删除，并提示用户通过启动器重启。

## 10. 验收标准

- 导入视频后立即可播放，历史中出现“未转写”记录。
- 刷新页面或重启应用后，未转写记录仍能打开。
- 重复导入不会产生重复目录或覆盖已有文字稿。
- 抖音、Bilibili、YouTube 和 TikTok 的单视频链接或分享文本可以解析并导入。
- 链接导入失败或终止后不残留任务临时文件，也不破坏当前历史记录。
- 同一平台视频再次链接导入时优先打开已有记录，不重复下载。
- 中文链接视频标题在 Windows 环境下导入后保持可读，不出现因错误解码产生的 `�` 替换字符。
- 相同链接媒体按内容哈希命中旧记录时，重新导入会修正展示名和来源元数据，同时保留已有文字稿。
- 链接记录完成转写或重新转写后仍保留全部公开来源字段。
- 点击“开始转写”后，进度只出现在文字稿区域。
- 转写期间导入、历史切换和删除均不可用。
- 终止或失败不会删除视频副本。
- 已完成记录重新转写前必须确认。
- 点击文字段落可以跳转视频，选择文字不会跳转。
- 拖动分隔条时文字稿始终延伸到窗口底部。
- 历史抽屉开关不会改变工作区尺寸。
- 页面不会因缓存或旧后台进程形成新版前端与旧版 API 的组合。

## 11. 后续候选能力

- 导出纯文本、Markdown、SRT 或 VTT；
- 批量导入与任务队列；
- 自定义语言和模型参数；
- 历史排序、标签和批量删除；
- 文字稿编辑及校对；
- 显示未转写媒体的准确时长。
